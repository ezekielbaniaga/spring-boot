spring:
  application:
    name: backend
  datasource:
    url: "jdbc:postgresql://localhost:5432/appdb"
    username: "devuser"
    password: "devpwd"
  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true

  logging:
    level:
      org.flywaydb: DEBUG

app:
  admin:
    username: ${ADMIN_USERNAME:admin}
    password: ${ADMIN_PASSWORD:}
  jwt:
    secret: ${JWT_SECRET:}
    expiration: 15m